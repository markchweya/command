# Root folder that Command is allowed to read/run in (your VS Code workspace root)
COMMAND_ROOT=.

# Optional: protect your MCP endpoint with a Bearer token (recommended)
COMMAND_TOKEN=replace-with-a-long-random-token

# Port for the HTTP server
PORT=8787